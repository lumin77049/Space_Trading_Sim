#include <stdio.h>

struct stock{
    float value;
    int stock_volume;
    char stock_tag[3]; // 3 character tag used as shorthand in any player-facing ui
};

int main(){
    printf("You need to finish this project !!!");
    return 0;

    // TODO: I *really* need to do something with this project lol
}
